!!!
%html
  %head
    %title Anonymous Feedback
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  
  %body.body
    = render partial: "layouts/navbar", locals: {new_post: @new_post, new_message: @new_message, users: @users, projects: @projects}
    .container-fluid
      %br
      - unless notice.nil?
        .alert.alert-success
          = notice
          %a.close{:href => "#", :data => {:dismiss => "alert"}} &times;
      - unless alert.nil?
        .alert.alert-error
          = alert
          %a.close{:href => "#", :data => {:dismiss => "alert"}} &times;
      .row-fluid
        - if current_user
          = yield
        - else
          = yield :new_user

